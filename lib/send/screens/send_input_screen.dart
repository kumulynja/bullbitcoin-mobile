import 'package:flutter/material.dart';

class SendInputScreen extends StatelessWidget {
  // Todo: pass the wallet type/payment method to the SendInputScreen
  const SendInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}

class BitcoinInputs extends StatelessWidget {
  const BitcoinInputs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class LiquidInputs extends StatelessWidget {
  const LiquidInputs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
